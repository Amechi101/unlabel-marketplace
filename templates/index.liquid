<!-- /templates/index.liquid -->
{% comment %}

  This is the homepage of your site.

  More info on global variables (available on all templates):
    - http://docs.shopify.com/themes/liquid-variables/global
{% endcomment %}

{% assign creators = linklists.creators %}
{% assign brands = linklists.brands %}

<section class="home__header">
    <div class="container">
        <div class="home__headerWrapper">
            <div class="visual visual--wideLandscape">
                <img src="{{ 'home-visual.jpg' | asset_url }}" alt="Intro">
            </div>
            <div class="home__headerTitleContainer">
                <h2 class="title title--headline">
                    <span class="u-block">Unla</span>
                    <span class="u-block">bel<span class="logo"><span class="logo__icon">{% include 'logo_dark' %}</span></span>
                    </span>
                </h2>
            </div>
            <div class="cols cols--m home__headerCols">
                <div class="col home__headerIntroContainer">
                    <p class="title title--medium">The best way to shop nice products from independent brands all over the world.</p>
                </div>
            </div>
        </div>
    </div>
</section>
<section class="home__creators color-context color-context--creators">
    <div class="home__CreatorsBg"></div>
    <div class="container">
        <div class="home__creatorsCols">
            <div class="home__creatorsCol home__creatorsCol--left">
                {% for obj in creators.links limit:1 offset:0 %}
                    <article class="home_creatorGridItem">
                        {% include 'portrait-image-block' %}
                    </article>
                {% endfor %}
                <div class="home__creatorsTitleContainer">
                    <h2 class="title title--caps">Our<br>
                    creators</h2>
                </div>
                <div class="home__creatorsCtaContainer">
                    <a class="cta cta--dark" href="{{ pages.our-creators.url }}">Discover more</a>
                </div>
                <div class="home__creatorsSubtitleContainer">
                    <div class="subtitle subtitle--medium home_creatorGridItem">
                        All our products are selected by our content creators for their quality, style and uniqueness.
                    </div>
                </div>
            </div>
            <div class="home__creatorsCol home__creatorsCol--middle">
                {% for obj in creators.links limit:1 offset:1 %}
                    <article class="home_creatorGridItem">
                        {% include 'portrait-image-block' %}
                    </article>
                {% endfor %}
            </div>
            <div class="home__creatorsCol home__creatorsCol--right u-desktop-only">
                {% for obj in creators.links limit:2 offset:2 %}
                    <article class="home_creatorGridItem">
                        {% include 'portrait-image-block' %}
                    </article>
                {% endfor %}
            </div>
        </div>
    </div>
</section>

<section class="home__brands color-context color-context--brands">
    <div class="container">
        <h2 class="home__brandsTitle title title--caps">Our<br class="u-desktop-only"/> Brands</h2>

        <div class="home__brandsCarousel carousel">
            <div class="cols cols--m cols--align-left cols--align-top">
                <div class="col brands__carouselColLeft">
                    <div class="swiper-container carousel_visualsSlider home__brandsVisualsSlider">
                        <div class="swiper-wrapper">
                        {% for brand in brands.links limit: 5 %}
                            <div class="swiper-slide">
                                <div class="visual visual--landscape">
                                    <img src="{{ brand.object.image | img_url: '1024x' }}" alt="{{ brand.title }}">
                                </div>
                            </div>
                        {% endfor %}
                        </div>
                    </div>
                </div>
                <div class="col brands__carouselColRight">
                    <div class="swiper-container carousel__backgroundsSlider home__brandsBgsSlider">
                        <div class="swiper-wrapper">
                            {% for brand in brands.links limit: 5 %}
                            <div class="swiper-slide">
                                <div class="carousel__bgSlide" style="{% cycle 'background-color: #EDDDDD;', 'background-color: #DEEDEE;', 'background-color: #DDE0ED;' %}"></div>
                            </div>
                            {% endfor %}
                        </div>
                    </div>
                    <div class="home__brandsCarouselContentWrapper">
                        <div class="swiper-container carousel_contentsSlider home__brandsContentsSlider">
                            <div class="swiper-wrapper">
                                {% for brand in brands.links limit: 5 %}
                                    <div class="swiper-slide">
                                        <h3 class="carousel_contentItem subtitle">{{ brand.title }}</h3>
                                        <div class="carousel__contentItem desc desc--small">{{ brand.object.description }}</div>
                                    </div>
                                {% endfor %}
                            </div>
                        </div>
                        <div class="home__brandsCarouselNav">
                            <a class="carousel__btn carousel__btn--prev" href="#prev"><span class="fonticon fonticon--smallBox fonticon--carouselArrowLeft"></span></a><a class="carousel__btn carousel__btn--next" href="#next"><span class="fonticon fonticon--smallBox fonticon--carouselArrowRight"></span></a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <nav class="home__brandsNav">
            <ul class="home__brandsNavList">
                <li class="home__brandsNavItem">
                    <a href="{{ collections.women.url }}" class="cta">See Women’s apparel</a>
                </li>
                <li class="home__brandsNavItem">
                    <a href="{{ pages.our-brands.url }}" class="cta cta--dark">See All Brands</a>
                </li>
                <li class="home__brandsNavItem">
                    <a href="{{ collections.men.url }}" class="cta">See Men’s apparel</a>
                </li>
            </ul>
        </nav>
    </div>
</section>

<section class="home__story">
    <div class="container">
        <div class="home__storyWrapper">
            <h2 class="subtitle">What we are</h2>
            <div class="desc home__storyDesc">Nullam maximus faucibus volutpat. Morbi ut erat nunc. Nulla cursus erat vitae dui dictum facilisis. Maecenas vel erat orci. Cras fringilla nunc in nibh tempor, et feugiat quam pellentesque.<br>Vivamus blandit bibendum pellentesque. Cras imperdiet commodo diam at hendrerit. Vivamus at semper erat, vel interdum lectus. Proin sagittis est sed interdum finibus. Suspendisse id vulputate est. Cras sed ligula nec nisl vehicula ultricies.</div>
        </div>
    </div>
</section>
<section class="home__products color-context color-context--products">
    <div class="container">
        <h2 class="title title--caps home__productsTitle">Start<br>shopping</h2>
    </div>
    <div class="grid2 home__productsGrid">
        <div class="grid__emptyMessage">
          {% for collection in collections %}
                {{ collection.title }}
           {% endfor %}
            
            <ol class="grid2__list">
                 {% for creators in creators.links %}
                 {% if creators.object.products.size > 0 %}
                        {% for product in creators.object.products limit:6 %}
                            {% if product.available %}
                                <li class="grid2__item">
                                    <article>
                                        <div class="grid__itemInner">
                                            {% include 'product-grid-item' %}
                                        </div>
                                    </article>
                                </li>
                            {% else %}
                                <p class="missing">No products for now.</p>
                            {% endif %}
                        {% endfor %}
                {% endfor %}
            </ol>
        </div>
    </div>
</section>
